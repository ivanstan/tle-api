language: php

cache:
  directories:
    - $HOME/.composer/cache

branches:
  only:
    - master

matrix:
  include:
    - php: 7.4

install:
  - composer update $COMPOSER_FLAGS

script:
  - composer test